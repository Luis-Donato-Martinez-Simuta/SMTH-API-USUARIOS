export const databaseConfig = {
    HOST : "127.0.0.1" || process.env.HOST,
    USERNAME : "nodejs" || process.env.USERNAME,
    PASSWPRD : "123" || process.env.PASSWPRD,
    NAME_DATABASE : "smtu_db_user" || process.env.NAME_DATABASE,
    PORT : 3306 || process.env.PORT,
}